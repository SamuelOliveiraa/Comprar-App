import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {},
  content: {
    paddingVertical: 15,
    gap: 6,
    flexDirection: "row",
    alignItems: "center"
  },
  trash: {
    marginLeft: "auto"
  }
});
